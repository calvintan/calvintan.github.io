{
  "license": "UNLICENSED",
  "scripts": {
    "deploy": "JEKYLL_ENV=production bundle exec jekyll build && gh-pages -d _site"
  },
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^2.1.1"
  }
}
